export type { TicketStatus } from "../constants/TicketStatus";
import type { TicketStatus } from "../constants/TicketStatus";
/** 类型层枚举镜像（供页面/store 以 import type 方式引用）。 */
export type TicketStatusValue = TicketStatus;
